[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "churnsystem"  # You can name this whatever you like
version = "0.1.0"
dependencies = [
    "pandas",
    "numpy",
    "scikit-learn"
]